// Corner-radius scale shared across light/dark themes.
// One consistent scale keeps shape language coherent (design rule: shape consistency).
const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  pill: 999,
};

export default radius;
